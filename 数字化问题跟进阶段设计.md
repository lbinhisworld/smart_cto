# 数字化问题跟进阶段设计

本文档定义数字化问题从创建到解决的全过程任务、阶段划分、任务目标与评价体系、阶段切换触发事件，以及聊天区的任务提示交互设计。

---

## 1. 大阶段与任务定义

### 1.1 大阶段（Major Stage）

| 阶段索引 | 阶段名称 | 说明 |
|----------|----------|------|
| 0 | 需求理解 | 基于客户输入提炼企业信息、商业画布与需求逻辑 |
| 1 | 工作流对齐 | 绘制价值流图，标注 IT 现状与痛点 |
| 2 | ITGap分析 | （预留）识别 IT 能力与业务需求的差距 |
| 3 | IT策略规划 | （预留）制定 IT 建设策略与路线图 |

### 1.2 任务与阶段归属

| 任务 ID | 任务名称 | 归属大阶段 | 子步骤索引 |
|---------|----------|------------|------------|
| task1 | 企业背景洞察 | 需求理解 | 0 |
| task2 | 商业画布加载 | 需求理解 | 1 |
| task3 | 需求逻辑构建 | 需求理解 | 2 |
| task4 | 绘制价值流 | 工作流对齐 | 0 |
| task5 | IT 现状标注 | 工作流对齐 | 1 |
| task6 | 痛点标注 | 工作流对齐 | 2 |

---

## 2. 任务目标设计

| 任务 | 目标描述 |
|------|----------|
| **企业背景洞察** | 基于客户输入的企业信息，提炼并结构化企业基本信息，为后续 BMC 与需求逻辑分析提供基础。 |
| **商业画布加载** | 基于企业基本信息，运用商业模式画布（BMC）框架生成结构化商业分析，识别客户细分、价值主张、渠道通路等九大模块。 |
| **需求逻辑构建** | 基于客户初步需求、企业基本信息、BMC 三个维度，产出需求背后的逻辑链条分析，明确行业特性、商业模式局限与业务痛点的因果关联。 |
| **绘制价值流** | 基于 enterprise_info、bmc_data、requirement_logic 生成业务核心价值流图，划分阶段与环节，标注执行角色与预估耗时。 |
| **IT 现状标注** | 结合需求逻辑，在价值流图每个环节节点标注该环节的 IT 支撑方式（手工/系统），区分纸质、excel 或具体系统名称。 |
| **痛点标注** | 结合需求逻辑，在价值流图每个环节节点提炼该环节涉及到的痛点，为后续 IT Gap 分析提供输入。 |

---

## 3. 任务评价体系设计

### 3.1 需求理解阶段

| 任务 | 评价标准 |
|------|----------|
| **企业背景洞察** | 成功提取并确认包含公司名称、信用代码、法人、成立时间、注册资本、经营范围等核心字段的结构化 JSON。 |
| **商业画布加载** | 生成完整的 BMC JSON，包含行业背景洞察、九大画布模块及业务痛点预判，用户确认后视为完成。 |
| **需求逻辑构建** | 输出结构化的需求逻辑 Markdown，包含行业底层逻辑、因果关联、深层动机及逻辑链条总结，用户确认后视为完成。 |

### 3.2 工作流对齐阶段

| 任务 | 评价标准 |
|------|----------|
| **绘制价值流** | 生成符合前端绘图要求的价值流 JSON，用户确认并完成「开始绘制价值流图」后视为完成。 |
| **IT 现状标注** | 每个环节均标注 itStatus，用户确认后视为完成。 |
| **痛点标注** | 每个环节均标注 painPoint（无明显痛点的环节不展示卡片），用户确认后视为完成。 |

### 3.3 完成状态存储

- **completedStages**：需求理解子步骤完成标记，`[0, 1, 2]` 分别对应企业背景洞察、商业画布加载、需求逻辑构建。
- **workflowAlignCompletedStages**：工作流对齐子步骤完成标记，`[0, 1, 2]` 分别对应绘制价值流、IT 现状标注、痛点标注。
- **currentMajorStage**：当前已到达的大阶段索引（0=需求理解，1=工作流对齐，2=ITGap分析，3=IT策略规划）。

---

## 4. 任务阶段切换的触发事件

### 4.1 需求理解 → 工作流对齐

**触发条件**：用户在工作流对齐占位页中，于聊天区确认价值流图设计 JSON 后，点击「开始绘制价值流图」卡片的「确认」按钮。

**触发动作**：
- `currentMajorStage` 更新为 1
- `workflowAlignCompletedStages` 写入 0（价值流已绘制）
- 工作区切换为价值流图展示视图
- 聊天区自动检测并展示「即将开始 IT 现状标注」或「即将开始痛点标注」等后续提示块（若满足前置条件）

### 4.2 需求理解子步骤完成

| 子步骤 | 触发事件 |
|-------|----------|
| 企业背景洞察 | 用户粘贴/输入企业信息 → 点击「提炼」→ 大模型解析 → 用户点击「确认」 |
| 商业画布加载 | 用户点击「生成 BMC」→ 大模型生成 → 用户点击「确认」 |
| 需求逻辑构建 | 用户点击「提炼需求逻辑」→ 大模型生成 → 用户点击「确认」 |

### 4.3 工作流对齐子步骤完成

| 子步骤 | 触发事件 |
|-------|----------|
| 绘制价值流 | 用户点击「我即将开始需求相关核心价值流图绘制」→ 确认 → 大模型生成价值流 JSON → 用户点击价值流卡片的「确认」→ 用户点击「开始绘制价值流图」的「确认」 |
| IT 现状标注 | 用户点击「即将开始 IT 现状标注」→ 确认 → 大模型为各环节标注 itStatus → 用户点击「确认」 |
| 痛点标注 | 用户点击「即将开始价值流图环节节点痛点标注」→ 确认 → 大模型为各环节标注 painPoint → 用户点击「确认」 |

### 4.4 执行操作意图（execute）

用户通过聊天区发送「重新进行 XXX」类指令，经意图提炼识别为 `intent=execute` 时，可直接触发对应任务：

| executeTaskId | 对应任务 | 示例指令 |
|--------------|----------|----------|
| task2 | 商业画布加载 | 重新生成 BMC |
| task3 | 需求逻辑构建 | 重新进行需求逻辑构建、重新构建需求逻辑 |
| task4 | 绘制价值流 | 重新绘制价值流 |
| task5 | IT 现状标注 | 重新进行 IT 现状标注 |
| task6 | 痛点标注 | 重新进行痛点标注 |

---

## 5. 聊天区的任务提示交互设计

### 5.1 设计原则

- **前置条件驱动**：仅当满足前置任务完成条件时，才展示下一任务的「即将开始」提示块。
- **用户确认**：每个「即将开始」类提示均需用户点击「确认」后才真正发起大模型调用。
- **状态反馈**：确认后按钮变为「已确认」并禁用，避免重复触发。

### 5.2 需求理解阶段提示块

| 提示内容 | 前置条件 | 确认后动作 |
|----------|----------|------------|
| 我将发起商业模式画布 BMC 生成 | 企业背景洞察已完成（basicInfo 已确认） | 调用 `generateBmcFromBasicInfo`，展示 BMC 卡片供用户确认 |
| 我即将开始提取需求逻辑 | 商业画布加载已完成（bmc 已确认） | 调用 `generateRequirementLogicFromInputs`，展示需求逻辑块供用户确认 |

### 5.3 工作流对齐阶段提示块

| 提示内容 | 前置条件 | 确认后动作 |
|----------|----------|------------|
| 我即将开始需求相关核心价值流图绘制 | currentMajorStage≥1，且 workflowAlignCompletedStages 不包含 0，且 basicInfo/bmc/requirementLogic 就绪 | 调用 `generateValueStreamFromInputs`，展示价值流 JSON 卡片 |
| 开始绘制价值流图 | 价值流 JSON 已生成且用户已确认价值流卡片 | 将 valueStream 写入问题记录，workflowAlignCompletedStages 写入 0，刷新工作区 |
| 即将开始 IT 现状标注 | workflowAlignCompletedStages 包含 0 且不包含 1，requirementLogic 就绪 | 调用 `generateItStatusAnnotation`，为各环节标注 itStatus |
| 即将开始价值流图环节节点痛点标注 | workflowAlignCompletedStages 包含 0、1 且不包含 2，requirementLogic 就绪 | 调用 `generatePainPointAnnotation`，为各环节标注 painPoint |

### 5.4 通用聊天交互

| 交互类型 | 说明 |
|----------|------|
| 输入框 | placeholder 为「输入消息…」，支持自由文本输入 |
| 意图提炼 | 用户发送消息后，系统调用 `extractUserIntentFromChat` 提炼意图，展示意图卡片（含确认/放弃） |
| 查询意图 | 确认后调用 `executeQueryIntent`，展示查询结果 |
| 讨论意图 | 确认后调用 `executeDiscussionIntent`，展示讨论回复 |
| 修改意图 | 确认后根据目标位置调用 `executeModificationIntent` 或价值流修改流程 |
| 执行意图 | 确认后根据 `executeTaskId` 直接触发对应任务（如 runBmcGeneration、runRequirementLogicConstruction 等） |

### 5.5 加载与状态文案

| 场景 | 文案 |
|------|------|
| 意图提炼中 | （无独立加载块，直接展示意图卡片） |
| 查询中 | 正在查询… |
| 讨论中 | 正在讨论… |
| 修改中 | 正在综合修改… |
| 提取客户基本信息 | 正在提取客户基本信息 json 数据 |
| 生成 BMC | 正在生成企业商业画布 BMC |
| 构建需求逻辑 | 正在基于最新客户初步需求、基本信息与 BMC 构建需求逻辑… |

---

## 6. 工作区与阶段导航

### 6.1 阶段导航条

- 展示四个大阶段：需求理解 → 工作流对齐 → ITGap分析 → IT策略规划
- 已完成的阶段显示 ✅，可点击回看
- 未到达的阶段不可点击
- 「回退」按钮支持回退到上一阶段（如需求逻辑回退、价值流回退等）

### 6.2 子步骤展示

- **需求理解**：企业背景洞察 → 商业画布加载 → 需求逻辑构建
- **工作流对齐**：绘制价值流 → IT 现状标注 → 痛点标注

子步骤以横向链条形式展示，已完成显示 ✅，当前进行中高亮。

---

## 7. 相关代码位置

| 功能 | 模块/常量 | 文件 |
|------|-----------|------|
| 任务定义 | FOLLOW_TASKS | main.js |
| 大阶段标签 | PROBLEM_DETAIL_MAJOR_STAGE_LABELS | main.js |
| 任务完成判断 | isTaskCompleted | main.js |
| 阶段切换 | updateDigitalProblemMajorStage、currentMajorStage | main.js |
| 提示块展示 | maybeShowBmcStartBlock、maybeShowRequirementLogicStartBlock、maybeShowValueStreamStartBlock、maybeShowItStatusStartBlock、maybeShowPainPointStartBlock | main.js |
| 执行意图映射 | runMap (task2~task6) | main.js |
